#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */

void print_alphabet(void);
{
	char bill;

	for (bill = 'a'; bill <= 'z' bill++)
		_putchar(bill);
	_putchar('\n');
	return (0);
}
