#include "main.h"

/**
 *  * _abs - compute absolute value of an integer
 *   * @n: is the integer in question
 *    * Return: the absolute value of the integer
 *     */

int _abs(int n)

{
		if (n >= 0)
					return (n);
			else
						return (-n);
}
